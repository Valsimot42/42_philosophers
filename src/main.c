#include "../philo.h"

// void *start_execution(void *arg)
// {

// }

int main(int ac, char **av)
{
	t_data		data;
	pthread_t	philo;

	philo = NULL;
	init_data(&data, ac, av);
	// pthread_mutex_init();
	// pthread_create();
	// pthread_join();
	// pthread_mutex_destroy();
}